type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Post {
  id: ID! @unique
  title: String!
  content: String!
  views: Int!
}

type Comment {
  id: ID! @unique
  comment: String!
}
